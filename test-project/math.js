function calculateSum(a, b) {
    return a + b;
}

function calculateProduct(a, b) {
    return a * b;
}

function main() {
    const x = 5;
    const y = 10;
    
    console.log(`Sum: ${calculateSum(x, y)}`);
    console.log(`Product: ${calculateProduct(x, y)}`);
}

main();
