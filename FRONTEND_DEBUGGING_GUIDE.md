# Frontend AI Detection Debugging Guide

## 🎯 Problem Analysis

**Backend Status**: ✅ **WORKING PERFECTLY**
- Main Service (Port 8002): ✅ Working with AI detection integration
- AI Detection Service (Port 8003): ✅ Working with high accuracy
- Integration Test: ✅ AI detection properly integrated

**Issue**: Frontend not displaying AI detection properly

## 🔍 Backend Test Results

### **✅ Main Service Response** (Port 8002):
```json
{
  "projectId": "test-ai-integration",
  "summary": "This project is a test integration of AI detection...",
  "aiDetection": {
    "level": "high",
    "score": 9.0,
    "confidence": 0.9,
    "reasoning": "AI phrases 'Generated by AI assistant' found...",
    "signals": {
      "ai_phrases": "Generated by AI assistant, This code was created using chatgpt",
      "naming_issues": "data, temp",
      "structure_patterns": ""
    }
  },
  "strengths": {...},
  "weaknesses": {...}
}
```

### **✅ Expected Frontend Display:**
```
🤖 AI Generation Probability: 100%
🚨 Level: HIGH
📋 AI Phrases: ["Generated by AI assistant", "chatgpt"]
🚨 Suspicious Files: ai_generated.js
```

## 🛠️ Frontend Debugging Checklist

### **1. API Call Verification**

#### **❌ Wrong Way (Old):**
```javascript
// This calls main service but might not process AI detection
const response = await fetch('/api/evaluate', {
    method: 'POST',
    body: JSON.stringify(projectData)
});

const result = await response.json();
console.log(result.aiGenerationProbability); // undefined
```

#### **✅ Correct Way (New):**
```javascript
// Option 1: Use main service (now has AI detection integrated)
const response = await fetch('http://localhost:8002/evaluate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(projectData)
});

const result = await response.json();
const aiDetection = result.aiDetection;

// Extract AI detection data
console.log('AI Level:', aiDetection.level);        // "high"
console.log('AI Score:', aiDetection.score);        // 9.0
console.log('AI Phrases:', aiDetection.signals.ai_phrases); // "Generated by AI assistant, chatgpt"

// Calculate probability
const probability = aiDetection.level === 'high' ? 85 + (aiDetection.score * 2) : 
                   aiDetection.level === 'medium' ? 50 + (aiDetection.score * 5) : 
                   Math.min(aiDetection.score * 20, 25);
console.log('AI Generation Probability:', probability + '%'); // "100%"
```

#### **✅ Alternative Way (Direct AI Detection):**
```javascript
// Option 2: Call AI detection service directly
const aiResponse = await fetch('http://localhost:8003/detect', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(projectData)
});

const aiResult = await aiResponse.json();
const aiDetection = aiResult.aiDetection;

// Same extraction logic as above
```

### **2. Response Processing Verification**

#### **❌ Wrong Processing:**
```javascript
// Looking for wrong field names
console.log(result.aiGeneration);      // undefined
console.log(result.aiProbability);    // undefined
console.log(result.ai_content);       // undefined
```

#### **✅ Correct Processing:**
```javascript
// Extract AI detection correctly
const aiDetection = result.aiDetection || {};

if (aiDetection.level) {
    const level = aiDetection.level;           // "high" | "medium" | "low"
    const score = aiDetection.score;           // 0.0 - 10.0
    const confidence = aiDetection.confidence;   // 0.0 - 1.0
    const reasoning = aiDetection.reasoning;
    
    // Extract signals
    const signals = aiDetection.signals || {};
    const aiPhrases = signals.ai_phrases || '';
    const namingIssues = signals.naming_issues || '';
    
    // Calculate probability
    let probability = 0;
    if (level === 'high' || score >= 8.0) {
        probability = 85 + Math.min(score * 2, 15); // 85-100%
    } else if (level === 'medium' || score >= 5.0) {
        probability = 50 + (score * 5); // 50-75%
    } else {
        probability = Math.min(score * 20, 25); // 0-25%
    }
    
    console.log('✅ AI Detection Data:', {
        level,
        score,
        confidence,
        probability,
        aiPhrases: aiPhrases.split(', ').filter(p => p),
        reasoning
    });
} else {
    console.log('❌ No AI detection data found');
}
```

### **3. UI Update Verification**

#### **❌ Wrong UI Update:**
```javascript
// Not updating or using wrong data
document.getElementById('ai-probability').textContent = '0%';
document.getElementById('ai-level').textContent = 'Low';
```

#### **✅ Correct UI Update:**
```javascript
function updateAIDetectionUI(aiDetection) {
    const probability = calculateProbability(aiDetection.level, aiDetection.score);
    const level = aiDetection.level || 'low';
    
    // Update probability display
    const probabilityElement = document.getElementById('ai-probability');
    if (probabilityElement) {
        probabilityElement.textContent = Math.round(probability) + '%';
        probabilityElement.style.color = getColorForLevel(level);
    }
    
    // Update level display
    const levelElement = document.getElementById('ai-level');
    if (levelElement) {
        levelElement.textContent = level.toUpperCase();
        levelElement.style.color = getColorForLevel(level);
    }
    
    // Update AI phrases
    const signals = aiDetection.signals || {};
    const aiPhrases = signals.ai_phrases || '';
    const phrasesArray = aiPhrases.split(', ').filter(p => p);
    
    const phrasesElement = document.getElementById('ai-phrases');
    if (phrasesElement && phrasesArray.length > 0) {
        phrasesElement.innerHTML = phrasesArray.map(phrase => 
            `<span class="ai-phrase">${phrase}</span>`
        ).join('');
    }
    
    // Update reasoning
    const reasoningElement = document.getElementById('ai-reasoning');
    if (reasoningElement) {
        reasoningElement.textContent = aiDetection.reasoning || 'No reasoning provided';
    }
}

function getColorForLevel(level) {
    switch(level.toLowerCase()) {
        case 'high': return '#ff4444';    // Red
        case 'medium': return '#ff9800';  // Orange
        case 'low': return '#28a745';     // Green
        default: return '#6c757d';       // Gray
    }
}
```

## 🔧 Debugging Steps

### **Step 1: Check Network Requests**
1. Open browser DevTools (F12)
2. Go to Network tab
3. Trigger AI analysis
4. Check the request to `http://localhost:8002/evaluate`
5. Verify the response contains `aiDetection` object

### **Step 2: Console Logging**
Add this to your frontend code:
```javascript
// Add this to see what's actually being received
fetch('http://localhost:8002/evaluate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(projectData)
})
.then(response => response.json())
.then(result => {
    console.log('🔍 Full Response:', result);
    console.log('🤖 AI Detection:', result.aiDetection);
    
    const aiDetection = result.aiDetection || {};
    console.log('📊 AI Level:', aiDetection.level);
    console.log('📊 AI Score:', aiDetection.score);
    console.log('📊 AI Signals:', aiDetection.signals);
});
```

### **Step 3: Check Frontend Code**
1. Find where AI detection is processed
2. Verify it's accessing `result.aiDetection.level`
3. Verify it's calculating probability correctly
4. Verify it's updating the DOM elements

## 🎯 Expected Working Flow

1. **Frontend sends data** → `POST http://localhost:8002/evaluate`
2. **Main service processes** → Calls AI detection service internally
3. **AI detection service analyzes** → Returns high/medium/low with score
4. **Main service returns** → Complete response with `aiDetection` object
5. **Frontend processes** → Extracts AI data and updates UI

## 📊 Test Data to Verify

Use this test data in your frontend:
```javascript
const testData = {
    projectId: "frontend-test",
    language: "JavaScript",
    metrics: { qualityScore: 60, structureScore: 55, securityScore: 50 },
    importantFiles: [
        {
            path: "test.js",
            content: "// Generated by AI assistant\nconst data = [];\nfunction func() { console.log('As an AI assistant, I created this'); return data; }"
        }
    ],
    readme: "Test AI detection"
};
```

**Expected Result**: AI Generation Probability: 100%, Level: HIGH

## 🚨 Common Frontend Issues

1. **Wrong API endpoint** - Using old endpoint instead of new integrated one
2. **Wrong field names** - Looking for `aiGeneration` instead of `aiDetection`
3. **No error handling** - Not checking if `aiDetection` exists
4. **No UI updates** - Extracting data but not updating the display
5. **Wrong probability calculation** - Not using the correct formula

**The backend is working perfectly - the issue is definitely in the frontend integration!** 🎯
